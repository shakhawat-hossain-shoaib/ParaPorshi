import 'package:flutter/material.dart';

class AppColors {
  // Brand colors
  static const primary = Color(0xFF0B8F4D); // Green
  static const secondary = Color(0xFF1F6FEB); // Blue

  // Backgrounds & surfaces
  static const background = Color(0xFFF7F9FB);
  static const card = Color(0xFFFFFFFF);
  static const surface = Color(0xFFFFFFFF);

  // Text
  static const textPrimary = Color(0xFF1E1E1E);
  static const textSecondary = Color(0xFF6B7280);
  static const textHint = Color(0xFF9CA3AF); // ✅ added, if something still uses it

  // Alerts / statuses
  static const alertDanger = Color(0xFFDC2626);
  static const alertWarning = Color(0xFFF97316);
  static const info = Color(0xFF0284C7);
  static const success = Color(0xFF16A34A);

  // Misc
  static const divider = Color(0xFFE5E7EB);
  static const accentYellow = Color(0xFFF4B41A);
}
